name: Recovery Build - Moto G 2025

on:
  workflow_dispatch:
    inputs:
      MANIFEST_URL:
        description: 'MANIFEST_URL'
        required: true
        default: 'https://github.com/PitchBlackRecoveryProject/manifest_pb.git'
      MANIFEST_BRANCH:
        description: 'MANIFEST_BRANCH'
        required: true
        default: 'android-12.1'
      DEVICE_TREE_URL:
        description: 'DEVICE_TREE_URL'
        required: true
        default: 'https://github.com/councilcj/android-device-tree-motog2025kansas'
      USER_NAME:
        description: 'councilcj'
        required: true
        default: 'AI-Collaborator'
      USER_EMAIL:
        description: 'councilcj344@gmail.com'
        required: true
        default: 'tester@example.com'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - name: Check Out
      uses: actions/checkout@v4

    - name: Cleanup
      uses: rokibhasansagar/slimhub_actions@main

    - name: Prepare the environment
      run: |
        sudo apt update
        sudo apt install git aria2 -y
        git clone https://gitlab.com/OrangeFox/misc/scripts
        cd scripts
        sudo bash setup/android_build_env.sh
        sudo bash setup/install_android_sdk.sh

    - name: Initialize repo
      run: |
        mkdir workspace
        cd workspace
        git config --global user.name "${{ github.event.inputs.USER_NAME }}"
        git config --global user.email "${{ github.event.inputs.USER_EMAIL }}"
        repo init --depth=1 -u ${{ github.event.inputs.MANIFEST_URL }} -b ${{ github.event.inputs.MANIFEST_BRANCH }}
      id: pwd

    - name: Repo Sync
      run: |
        repo sync -c --no-clone-bundle --no-tags --optimized-fetch --prune --force-sync -j$(nproc --all)
      working-directory: workspace

    - name: Clone device tree
      run: |
        git clone ${{ github.event.inputs.DEVICE_TREE_URL }} ./device/motorola/kansas
      working-directory: workspace

    - name: Set Swap Space
      uses: pierotofy/set-swap-space@master
      with:
        swap-size-gb: 12

    - name: Building recovery
      run: |
        source build/envsetup.sh
        export ALLOW_MISSING_DEPENDENCIES=true
        
        # Lunch the device
        lunch omni_kansas-eng || lunch omni_boot-eng
        
        # --- THE NINJA FIX ---
        # Manually staging files into the 'out' directory so Ninja doesn't panic
        mkdir -p out/target/product/kansas/
        cp device/motorola/kansas/dtb.img out/target/product/kansas/dtb.img
        cp device/motorola/kansas/dtbo.img out/target/product/kansas/dtbo.img
        cp device/motorola/kansas/kernel out/target/product/kansas/kernel
        
        # Build the bootimage
        # We use 'm' instead of 'make bootimage' for better dependency handling in PBRP
        m bootimage -j$(nproc --all)
      working-directory: workspace

    - name: Upload to Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          workspace/out/target/product/kansas/boot.img
        name: kansas-PBRP-Build
        tag_name: ${{ github.run_id }}
        body: "Custom Recovery Build for Moto G 2025 (kansas)"
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
